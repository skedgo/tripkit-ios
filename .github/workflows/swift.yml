name: Swift

on:
  push:
    branches: [ main, spm ]
  pull_request:
    branches: [ main, spm ]

jobs:
  build:

    runs-on: macos-11

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v --target TripKit
    # - name: Run tests
    #   run: swift test -v
